extends layout

block body
    div(ng-controller='AppCtrl')
        div(class=['navbar', 'navbar-inverse', 'navbar-fixed-top'], role='navigation')
            div.container
                div.navbar-header
                    a.navbar-brand(href='#') Project Name
                div.navbar-collapse.collapse
                    ul.nav.navbar-nav
                        li
                            a(href='view1') view1
                        li
                            a(href='view2') view2
        h2 Hello {{name}}

        div(ng-view)

        p #{content} this line shows how we pass information from node side to jade template side

        div
            | Angular seed app: v
            span(app-version)
